name: Release GD

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release:
    if: startsWith(github.event.release.tag_name, 'gd-v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Package addon
        run: |
          set -euo pipefail
          test -f gd/addon/plugin.cfg
          repo_owner="${GITHUB_REPOSITORY%%/*}"
          addon_dir="@${repo_owner}_${GITHUB_REPOSITORY#*/}"
          package_root="dist/${addon_dir}"
          mkdir -p "$package_root"
          cp gd/addon/plugin.cfg gd/addon/plugin.gd "$package_root/"
          cp gd/addon/*.uid "$package_root/" 2>/dev/null || true
          if [ -f gd/addon/autoload.gd ]; then cp gd/addon/autoload.gd "$package_root/"; fi
          if [ -d gd/addon/src ]; then cp -R gd/addon/src "$package_root/"; fi
          (cd "$package_root" && zip -r "../${addon_dir}.zip" .)

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: dist/*.zip

      - name: Install GDAM
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/bin"
          curl -fsSL https://raw.githubusercontent.com/aviorstudio/gdam/main/scripts/install_cli.sh | INSTALL_DIR="$RUNNER_TEMP/bin" sh
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"

      - name: Publish to GDAM
        env:
          GDAM_SECRET_KEY: ${{ secrets.GDAM_SECRET_KEY }}
        run: |
          set -euo pipefail
          if [ -z "${GDAM_SECRET_KEY:-}" ]; then
            echo 'Missing required secret: GDAM_SECRET_KEY' >&2
            exit 1
          fi
          tag="${{ github.event.release.tag_name }}"
          version="${tag#gd-v}"
          if [ "$version" = "$tag" ]; then
            echo 'Release tag must look like gd-v1.2.3.' >&2
            exit 1
          fi
          repo_name="${GITHUB_REPOSITORY#*/}"
          asset_name="@${GITHUB_REPOSITORY/\//_}.zip"
          gdam publish "@aviorstudio/${repo_name}" "$version" "$tag" "$asset_name"
